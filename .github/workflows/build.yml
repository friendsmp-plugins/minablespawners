name: Build

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    permissions:
      contents: write
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '26'
      - uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.1'
      - run: gradle --no-daemon build
      - uses: actions/upload-artifact@v4
        with:
          name: minablespawners-paper
          path: build/libs/minablespawners.jar
      - run: gradle --no-daemon build
        working-directory: fabric
      - uses: actions/upload-artifact@v4
        with:
          name: minablespawners-fabric
          path: fabric/build/libs/minablespawners.jar
      - uses: softprops/action-gh-release@v2
        if: github.event_name == 'push'
        with:
          tag_name: build-${{ github.run_number }}
          name: Latest build
          body: Latest minable spawners build.
          files: |
            build/libs/minablespawners.jar
            fabric/build/libs/minablespawners.jar
